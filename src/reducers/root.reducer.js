import { combineReducers } from 'redux';
// TODO: Rename `firstReducer` to any other reducer you will add to your app
import firstReducer from './init.reducer';

export default combineReducers({
  firstReducer
})